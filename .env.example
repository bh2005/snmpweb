# Benutzer/Rollen werden in users.yaml gepflegt (siehe users.example.yaml),
# nicht mehr per Umgebungsvariable.

# Optional: nur nötig, wenn MIB-Uploads Basis-MIBs von mibs.pysnmp.com per
# Proxy nachladen müssen.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=
