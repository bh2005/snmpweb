services:
  snmpweb:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: snmpweb
    restart: unless-stopped
    ports:
      - "8082:8000"
    env_file:
      - .env
    # Für UDP-Portscans (-sU) statt des Standard-TCP-Connect-Scans würde
    # zusätzlich NET_RAW/NET_ADMIN benötigt, z.B.:
    # cap_add:
    #   - NET_RAW
    #   - NET_ADMIN
